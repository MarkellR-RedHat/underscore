# written by rot: the schedule and a person both write this state; this is how it merges.
# union needs no setup; ours needs `git config merge.ours.driver true` once per clone,
# and without it git falls back to a normal conflict, which is safe, just noisier.
# append-only: keep both sides' runs
history.jsonl merge=union
# regenerated by rot record; a conflict here is noise
expect/*.json merge=ours
# regenerated by rot run
reports/** merge=ours
# regenerated by rot run
badges/*.svg merge=ours
